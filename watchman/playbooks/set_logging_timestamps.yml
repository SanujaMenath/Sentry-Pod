- name: Fix logging time
  hosts: allHosts
  gather_facts: false

  tasks:
    - name: Set local time for syslog
      cisco.ios.ios_config:
        lines:
          - service timestamps log datetime localtime
